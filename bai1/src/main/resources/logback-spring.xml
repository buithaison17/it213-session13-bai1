<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Appender ghi vào System.err để tránh Stdio Pollution trên System.out -->
    <appender name="STDERR_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Áp dụng cho Root và các package nhạy cảm -->
    <root level="INFO">
        <appender-ref ref="STDERR_CONSOLE"/>
    </root>

    <logger name="org.springframework" level="INFO"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="io.modelcontextprotocol" level="DEBUG"/>
</configuration>